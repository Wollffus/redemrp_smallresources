Trigger = {
    AIMASSIST= true, -- Disables AA on controllers
    BATHING = true, -- Enable bath houses
    BLIPS = true, -- Create blips for locations
    COORDS = true, -- Displays player coordinates on screen using /coords
    DOORSOPEN = true, -- Enables more open doors around the map
    GUNTWIRL = true, -- Enables several gun tricks using the command /guntricks
    HANDCUFFS = true, -- Enables handcuff menu for players with item 'handcuffs'
    HANDSUP = true, -- Enables the handsup emote by using X by default
    IDGUN = true, -- Enables the command /idgun to identify objects hash and position
    JOBDISPLAY = true, -- Enables the command /showjob to display players jobs
    PLAYERID = true, -- Toggles player ID abvove head with Z by default
    POINTING = true, -- Enables pointing using the key J by default
    REVIVE = true, -- Enables item snakeoil so players can revive (must be using WH_Redemrp_Doctor resource)
    STAMINA = false, -- Unlimited gold ring stamina bar
}